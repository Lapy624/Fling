-- INVISIBLE + GOD MODE v3.0 (DRAGGABLE ICON)
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "IGui"
screenGui.ResetOnSpawn = false
screenGui.Parent = LocalPlayer.PlayerGui

-- ===== ИКОНКА (ПЕРЕТАСКИВАЕМАЯ) =====
local icon = Instance.new("TextButton")
icon.Size = UDim2.new(0, 55, 0, 55)
icon.Position = UDim2.new(0, 10, 0, 10)
icon.BackgroundColor3 = Color3.fromRGB(25,25,35)
icon.BorderSizePixel = 0
icon.Text = "👻"
icon.TextScaled = true
icon.TextColor3 = Color3.fromRGB(0,255,200)
icon.Parent = screenGui

local dragging = false
local dragStartPos = nil
local iconStartPos = nil

icon.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStartPos = input.Position
        iconStartPos = icon.Position
    end
end)

icon.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
    end
end)

icon.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseMovement) then
        local delta = input.Position - dragStartPos
        icon.Position = UDim2.new(
            iconStartPos.X.Scale,
            iconStartPos.X.Offset + delta.X,
            iconStartPos.Y.Scale,
            iconStartPos.Y.Offset + delta.Y
        )
    end
end)

-- ===== МЕНЮ =====
local menu = Instance.new("Frame")
menu.Size = UDim2.new(0, 180, 0, 100)
menu.Position = UDim2.new(0, 70, 0, 10)
menu.BackgroundColor3 = Color3.fromRGB(20,20,35)
menu.BorderSizePixel = 0
menu.Visible = false
menu.Parent = screenGui

local btn = Instance.new("TextButton")
btn.Size = UDim2.new(0.9,0,0,35)
btn.Position = UDim2.new(0.05,0,0,10)
btn.BackgroundColor3 = Color3.fromRGB(40,40,60)
btn.BorderSizePixel = 0
btn.Text = "👻 INVISIBLE"
btn.TextColor3 = Color3.fromRGB(200,200,220)
btn.Font = Enum.Font.Gotham
btn.TextSize = 14
btn.Parent = menu

local btn2 = Instance.new("TextButton")
btn2.Size = UDim2.new(0.9,0,0,35)
btn2.Position = UDim2.new(0.05,0,0,50)
btn2.BackgroundColor3 = Color3.fromRGB(40,40,60)
btn2.BorderSizePixel = 0
btn2.Text = "🛡️ GOD MODE"
btn2.TextColor3 = Color3.fromRGB(200,200,220)
btn2.Font = Enum.Font.Gotham
btn2.TextSize = 14
btn2.Parent = menu

icon.MouseButton1Click:Connect(function()
    menu.Visible = not menu.Visible
end)

-- ===== СОСТОЯНИЯ =====
local invisible = false
local god = false

-- ===== ПРИМЕНЕНИЕ НЕВИДИМОСТИ =====
local function applyInvisible(char)
    if not char then return end
    for _, part in ipairs(char:GetDescendants()) do
        if part:IsA("BasePart") then
            part.Transparency = invisible and 1 or 0
            part.CanCollide = not invisible
        end
    end
end

-- ===== ПРИМЕНЕНИЕ GOD MODE =====
local function applyGod(char)
    if not char then return end
    local humanoid = char:FindFirstChild("Humanoid")
    if humanoid then
        if god then
            humanoid.MaxHealth = 1e9
            humanoid.Health = 1e9
            humanoid.BreakJointsOnDeath = false
        else
            humanoid.MaxHealth = 100
            humanoid.Health = 100
            humanoid.BreakJointsOnDeath = true
        end
    end
end

-- ===== ПРИ РЕСПАВНЕ =====
LocalPlayer.CharacterAdded:Connect(function(char)
    char:WaitForChild("Humanoid")
    task.wait(0.1)
    applyInvisible(char)
    applyGod(char)
end)

-- ===== INVISIBLE TOGGLE =====
btn.MouseButton1Click:Connect(function()
    invisible = not invisible
    btn.BackgroundColor3 = invisible and Color3.fromRGB(0,150,200) or Color3.fromRGB(40,40,60)
    applyInvisible(LocalPlayer.Character)
end)

-- ===== GOD MODE TOGGLE =====
btn2.MouseButton1Click:Connect(function()
    god = not god
    btn2.BackgroundColor3 = god and Color3.fromRGB(0,150,200) or Color3.fromRGB(40,40,60)
    applyGod(LocalPlayer.Character)
end)

print("👻 INVISIBLE + GOD MODE v3.0 LOADED")
